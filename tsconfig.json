{
  "compilerOptions": {
    // === 编译输出控制 ===
    "target": "ES5", // 虽然 Babel 会转，但 TS 检查时假设你的运行环境是 ES5
    "module": "ESNext", // 保持 import/export 语句
    "jsx": "react-jsx", // 对应 Babel 的 runtime: automatic

    // === 模块解析策略 ===
    "moduleResolution": "node", // 像 Node.js 一样查找 node_modules
    "esModuleInterop": true, // 允许 default import 非 ESM 模块

    // === 严格模式 ===
    "strict": true,             // 开启所有严格检查 (noImplicitAny 等)
    "skipLibCheck": true,       // 跳过 node_modules 里库文件的类型检查 (提速)

    // === 路径别名 ====
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"]
}
