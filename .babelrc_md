presets (预设):
Babel 本身是个空壳，插件（Plugins）才是干活的。为了不让你手动安装几十个插件（比如“箭头函数转换插件”、“Class转换插件”），Babel 把常用插件打包成了 preset。
执行顺序：从下往上（这是个著名的坑）。也就是说，先剥离 TypeScript，再处理 React JSX，最后处理 ES6+ 语法。

@babel/preset-typescript:
作用：它极其简单粗暴，它并不检查类型！它只是单纯地把所有的 TypeScript 类型注解（比如 : string, interface）像删注释一样删掉。
为什么用它不用 ts-loader？：因为它快。Babel 只做词法分析，不做类型推导，速度极快。类型检查我们交给 IDE 和独立的 fork-ts-checker-webpack-plugin 进程去跑。

@babel/preset-react:
作用：把 <div /> 转换成 React.createElement('div')。
关键配置 { "runtime": "automatic" }:
老版 (Classic): 你必须在每个文件顶部写 import React from 'react'，否则报错。
新版 (Automatic): React 17+ 引入。Babel 会自动注入类似 import { jsx as _jsx } from 'react/jsx-runtime' 的代码。这就是为什么现在的项目里，你不需要显式引入 React 就能写组件。

@babel/preset-env:
作用：智能转换 ES6+ (const, let, async/await) 到 ES5。
潜台词：它非常智能，会读取 package.json 里的 browserslist 配置。如果你只支持最新的 Chrome，它几乎什么都不转（原生支持）；如果你要支持 IE11，它会把代码转译得面目全非。